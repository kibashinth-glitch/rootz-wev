body { margin:0; font-family:Arial; background:#050505; color:white; }

header {
  display:flex; justify-content:space-between;
  padding:20px; background:black; align-items:center;
}

.logo { display:flex; align-items:center; gap:10px; }
.logo img { width:40px; }

.hero { text-align:center; padding:80px 20px; }
.hero-logo { width:80px; }

.btn {
  background:white; color:black; padding:10px 20px;
  text-decoration:none; border-radius:5px; display:inline-block;
}

.products {
  display:flex; flex-wrap:wrap; gap:20px;
  justify-content:center; padding:40px;
}

.card {
  width:260px; background:#111;
  padding:15px; border-radius:10px;
}

/* gallery */
.product-gallery { position:relative; }

.main-img {
  width:100%;
  height:260px;
  object-fit:cover;
  border-radius:10px;
}

.arrow {
  position:absolute; top:45%;
  background:rgba(0,0,0,0.6);
  color:white; border:none; cursor:pointer;
}

.left { left:5px; }
.right { right:5px; }

.thumbs { display:flex; gap:5px; margin-top:8px; }

.thumbs img {
  width:50px; height:50px;
  object-fit:cover;
  opacity:0.6; cursor:pointer;
}

.thumbs img.active {
  opacity:1;
  border:2px solid white;
}

footer,#contact { text-align:center; padding:30px; }